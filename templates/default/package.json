{
	"name": "{{PROJECT_NAME}}",
	"version": "0.1.0",
	"module": "src/index.ts",
	"type": "module",
	"scripts": {
		"dev": "bun run src/index.ts",
		"check": "biome check .",
		"lint": "biome lint .",
		"format": "biome format --write ."
	},
	"dependencies": {
		"@total-typescript/ts-reset": "^0.6.1"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"typescript": "^5.7.3"
	},
	"engines": {
		"bun": ">=1.0.0"
	}
}
